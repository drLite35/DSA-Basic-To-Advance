#include<iostream>
#include<algorithm>
#include<math.h>
#include<stdio.h>
using namespace std;
 
int main(){
   int len=0 , wid=0;
   cin>>len>>wid;
   cout<<(2*(len+wid));
   cout<<(len*wid);
   //everything is inside the main class and it is called monolithic programming
return 0;
}